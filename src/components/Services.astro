---
import { useTranslations } from '../i18n/utils';

interface Props {
  lang?: 'es' | 'en';
}

const { lang = 'es' } = Astro.props;
const t = useTranslations(lang);
---

<section id="servicios" class="py-24 px-4 lg:px-20 bg-base-100">
  <div class="max-w-4xl mx-auto text-center mb-16 animate-on-scroll">
    <h2 class="text-4xl font-black mb-4 uppercase tracking-tight">
      {t('services.title')}
    </h2>
    <p class="text-lg opacity-70">
      {t('services.subtitle')}
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-7xl mx-auto">
    <div
      class="card bg-base-200 shadow-xl border-t-4 border-primary hover:scale-[1.02] transition-transform duration-300 animate-on-scroll"
    >
      <div class="card-body">
        <div class="text-4xl mb-2">⚡</div>
        <h3 class="card-title text-2xl font-bold">{t('services.landing.title')}</h3>
        <p class="text-sm opacity-80 min-h-[60px]">
          {t('services.landing.desc')}
        </p>
      </div>
    </div>

    <div
      class="card bg-base-200 shadow-xl border-t-4 border-secondary hover:scale-[1.02] transition-transform duration-300 animate-on-scroll"
    >
      <div class="card-body">
        <div class="text-4xl mb-2">🎨</div>
        <h3 class="card-title text-2xl font-bold">{t('services.portfolio.title')}</h3>
        <p class="text-sm opacity-80 min-h-[60px]">
          {t('services.portfolio.desc')}
        </p>
      </div>
    </div>

    <div
      class="card bg-base-200 shadow-xl border-t-4 border-accent hover:scale-[1.02] transition-transform duration-300 animate-on-scroll"
    >
      <div class="card-body">
        <div class="text-4xl mb-2">✍️</div>
        <h3 class="card-title text-2xl font-bold">{t('services.blog.title')}</h3>
        <p class="text-sm opacity-80 min-h-[60px]">
          {t('services.blog.desc')}
        </p>
      </div>
    </div>
  </div>

  <div
    class="mt-12 flex flex-wrap justify-center items-center gap-12 opacity-30 font-black text-2xl uppercase tracking-widest"
  >
    <span>Astro</span>
    <span>Tailwind</span>
    <span>React</span>
    <span>TypeScript</span>
  </div>

  <div class="mt-32">
    <div class="max-w-4xl mx-auto text-center mb-16">
      
      <h2 class="text-3xl font-black mb-4 uppercase tracking-tight">
        {t('services.infra.title')}
      </h2>
      <p class="text-lg opacity-70">
        {t('services.infra.desc')}
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-6xl mx-auto">
      <div
        class="card bg-neutral text-neutral-content shadow-2xl border border-white/5 group"
      >
        <div class="card-body">
          <div class="flex items-start justify-between mb-4">
            <h3 class="card-title text-2xl font-bold text-secondary">
              {t('services.managed.title')}
            </h3>
            <div class="text-3xl group-hover:rotate-12 transition-transform">
              🚀
            </div>
          </div>

          <p class="text-sm opacity-80 my-4">
            {t('services.managed.desc')}
          </p>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-y-4 gap-x-6">
            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.domain')}</span>
            </div>

            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.email')}</span>
            </div>

            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.ssl')}</span>
            </div>

            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.speed')}</span>
            </div>

            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.protection')}</span>
            </div>

            <div class="flex items-center gap-3 text-sm">
              <span class="text-secondary font-bold text-lg">✦</span>
              <span>{t('services.features.maintenance')}</span>
            </div>
          </div>
        </div>
      </div>

      <div
        class="card bg-neutral text-neutral-content shadow-2xl border border-white/5 group"
      >
        <div class="card-body">
          <div class="flex items-start justify-between mb-4">
            <h3 class="card-title text-2xl font-bold text-primary">
              {t('services.security.title')}
            </h3>
            <div class="text-3xl group-hover:scale-110 transition-transform">
              🛡️
            </div>
          </div>
          <p class="text-sm opacity-80 my-4">
            {t('services.security.desc')}
          </p>
          <div class="space-y-4">
            <div class="flex items-center gap-3 text-sm">
              <span class="text-primary font-bold text-lg">✦</span>
              <span>{t('services.security.ddos')}</span>
            </div>
            <div class="flex items-center gap-3 text-sm">
              <span class="text-primary font-bold text-lg">✦</span>
              <span>{t('services.security.global')}</span>
            </div>
            <div class="flex items-center gap-3 text-sm">
              <span class="text-primary font-bold text-lg">✦</span>
              <span>{t('services.security.images')}</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div
      class="mt-12 flex flex-wrap justify-center items-center gap-12 opacity-30 font-black text-2xl uppercase tracking-widest"
    >
      <span>Vercel</span>
      <span>Netlify</span>
      <span>Cloudflare</span>
      <span>DNS</span>
    </div>
  </div>
</section>
